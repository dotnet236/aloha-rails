Description:
    Installs the aloha-config.coffee file that contains global Aloha configurations.

Example:
    rails generate aloha:config 

    This will create:
        app/assets/javascripts/aloha-config.coffee
